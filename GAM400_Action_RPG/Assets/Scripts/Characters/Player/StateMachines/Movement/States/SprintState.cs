namespace BattleZZang
{
    public class SprintState : PlayerMoveState
    {
        public SprintState(PlayerMoveStateMachine stateMachine, CharacterMoveData moveData) : base(stateMachine, moveData)
        {
        }
    }
}
